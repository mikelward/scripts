#!/bin/bash
. "$HOME"/.shrc
WORKSTATION="$(workstation)"
if test -z "$WORKSTATION" || test "$HOSTNAME" = "$WORKSTATION"; then
  terminal
else
  terminal -e "$SHELL -c '. $HOME/.shrc; need_auth && auth; ssh $WORKSTATION'"
fi
