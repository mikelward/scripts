#!/bin/sh
# copies UNIX configuration files to other hosts

error()
{
	echo "$@" 1>&2
}

run()
{
	if test "$simulate" -gt 0
	then
		echo "$@"
	else
		"$@"
	fi
	return $?
}

scp()
{
	if test "$verbose" -gt 0
	then
		scp -Bq "$@"
	else
		scp -Bq "$@" 2>/dev/null
	fi
	return $?
}

ssh()
{
	if test "$verbose" -gt 0
	then
		ssh "$@"
	else
		ssh "$@" 2>/dev/null
	fi
	return $?
}

read_defaults()
{
	conffile="$HOME"/conf.tar.gz
	hosts=
	verbose=0
	test -f "$HOME"/.confdist && . "$HOME"/.confdist
}

read_options()
{
	while getopts ":v" flag
	do
		case $flag in
		c)
			conffile="$OPTARG"
			;;
		v)
			verbose=1
			;;
		*)
			error "$0: Unknown option $flag"
			exit 2
		esac
	done

	shift `expr $OPTIND - 1`

	test $# -gt 0 && hosts="$@"
}

copy_to()
{
	typeset host=$1
	printf "Copying to $host... "
	scp $conffile $host:~
	if test $? -ne 0
	then
		printf "failure\n"
		continue
	fi
	printf "success\n"
}

install_on()
{
	typeset host=$1
	printf "Installing on $host... "
	ssh $host "mkdir bin"
	scp $HOME/bin/confinst $host:bin
	if test $? -ne 0
	then
		printf "failure copying install script to $host:bin\n"
		continue
	fi

	ssh $host "bin/confinst"
	if test $? -ne 0
	then
		printf "failure running remote install script\n"
	else
		printf "success\n"
	fi
}

distribute_from()
{
	typeset host=$1
	ssh $host confdist
}

read_defaults
read_options "$@"

for host in $hosts
do
	copy_to $host && install_on $host && distribute_from $host
done

