name: codex-review-check
on:
  push:
  pull_request_target:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  codex-review-check:
    uses: mikelward/codex-review/.github/workflows/check-consumer.yml@main
